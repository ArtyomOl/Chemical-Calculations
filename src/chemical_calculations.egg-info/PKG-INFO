Metadata-Version: 2.4
Name: chemical-calculations
Version: 1.0.0
Summary: Приложение для расчётов физико-химических процессов в газах
Author: Артём Оленев, Владислав Коровин
Classifier: Development Status :: 4 - Beta
Classifier: Intended Audience :: Science/Research
Classifier: Programming Language :: Python :: 3.9
Classifier: Programming Language :: Python :: 3.10
Classifier: Programming Language :: Python :: 3.11
Classifier: Programming Language :: Python :: 3.12
Requires-Python: >=3.9
Description-Content-Type: text/markdown
Requires-Dist: PyQt5>=5.15.0
Requires-Dist: sympy>=1.12
Requires-Dist: matplotlib>=3.7.0
Requires-Dist: pandas>=2.0.0
Requires-Dist: numpy>=1.24.0
Requires-Dist: PyMySQL>=1.1.0
Requires-Dist: pydantic>=2.0.0
Requires-Dist: pydantic-settings>=2.0.0
Provides-Extra: dev
Requires-Dist: black>=23.0.0; extra == "dev"
Requires-Dist: isort>=5.12.0; extra == "dev"
Requires-Dist: ruff>=0.1.0; extra == "dev"
Requires-Dist: mypy>=1.5.0; extra == "dev"
Requires-Dist: pytest>=7.4.0; extra == "dev"
Dynamic: requires-python

# Chemical Calculations

Приложение для расчётов и моделирования физико-химических процессов в бинарных растворах с визуализацией результатов.

**Авторы:** Артём Оленев, Владислав Коровин (МГТУ им. Н.Э. Баумана)

## Возможности

- 📊 Импорт экспериментальных данных (CSV/Excel)
- 🗄️ Локальная база данных (SQLite) или удалённая (MySQL)
- 🔍 Фильтрация экспериментов по типам веществ
- 🧮 Конструктор математических моделей
- ⚡ 5 методов оптимизации параметров:
  - Имитация отжига
  - Гаусса-Зейделя
  - Хукка-Дживса
  - Антиградиент (градиентный спуск)
  - Ньютона
- 📈 Визуализация с наложением экспериментальных точек
- 🎯 Мультистарт для поиска глобального минимума
- 💾 Сохранение результатов расчётов

## Технологии

- **Python 3.9+**
- **UI:** PyQt5
- **Математика:** SymPy, NumPy, Matplotlib
- **Данные:** Pandas, SQLite/MySQL
- **Конфигурация:** Pydantic Settings
- **Качество кода:** Black, Ruff, isort, mypy

## Архитектура

Проект следует принципам Clean Architecture с разделением на слои:

```
src/
├── core/              # Конфигурация приложения
├── domain/            # Доменные модели
├── infrastructure/    # Работа с БД и внешними системами
├── calculation/       # Математические вычисления
├── services/          # Бизнес-логика
└── ui/               # Пользовательский интерфейс
    ├── dialogs/      # Диалоговые окна
    └── widgets/      # Виджеты
```

## Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd Chemical-Calculations
```

### 2. Создание виртуального окружения

```bash
python -m venv .venv

# macOS/Linux
source .venv/bin/activate

# Windows
.venv\Scripts\activate
```

### 3. Установка зависимостей

```bash
pip install -e .

# Для разработки
pip install -e ".[dev]"
```

### 4. Конфигурация

Скопируйте `.env.example` в `.env` и настройте параметры:

```bash
cp .env.example .env
```

Пример `.env`:

```env
DB_TYPE=sqlite
SQLITE_PATH=db/main_database.db

# Для MySQL
# DB_TYPE=mysql
# MYSQL_HOST=localhost
# MYSQL_PORT=3306
# MYSQL_USER=root
# MYSQL_PASSWORD=your_password
# MYSQL_DATABASE=chemical_calc
```

## Запуск

```bash
chem-calc
```

Или напрямую через Python:

```bash
python -m src.main
```

## Использование

### Импорт экспериментов

1. Перейдите на вкладку **Эксперименты**
2. Нажмите **Добавить**
3. Выберите CSV/Excel файл

**Формат файла:**
- Строка 1: Названия веществ (столбцы 1 и 2)
- Строка 2: Температура (K) и давление (опционально)
- Далее: Данные (x2, PkPa, y1, y2, GEJ и т.д.)

### Создание модели

1. Вкладка **Модели** → **Добавить**
2. Заполните поля:
   - **Название:** Уникальное имя модели
   - **Уравнение:** Математическое выражение (SymPy синтаксис)
   - **Вычисляемый параметр:** Имя столбца из данных (например, `GEJ`)
   - **Аргумент:** Переменная по оси X (например, `x` или `x2`)
   - **Начальные параметры:** По одному на строку в формате `ключ: значение`

**Пример уравнения:**
```
8.31*temp*x*(1-x)*((1-x)*a12 + a21*x)
```

### Расчёт

1. Выберите эксперимент → ПКМ → **Рассчитать**
2. Выберите модель и метод оптимизации
3. Введите начальные значения параметров:
   ```
   a12: 1.0, a21: 2.0
   ```
4. Для мультистарта:
   - Включите чекбокс **Мультистарт**
   - Укажите диапазоны и количество запусков

### Фильтрация

Используйте фильтры для поиска экспериментов:

```
Alcohol          # Все спирты
Any not Alcohol  # Всё кроме спиртов
Alkane Alcohol   # Алканы и спирты
```

## Разработка

### Форматирование кода

```bash
black src/
isort src/
```

### Проверка качества

```bash
ruff check src/
mypy src/
```

### Запуск тестов

```bash
pytest
```

## Структура базы данных

### Таблицы

- **experiments** - Экспериментальные данные
- **elements** - Химические элементы и их классификация
- **models** - Математические модели
- **articles** - Научные статьи
- **attempts** - История расчётов

## Методы оптимизации

### Имитация отжига
Глобальный метод, хорош для сложных функций с множеством локальных минимумов.

### Гаусса-Зейделя
Координатный спуск, прост и надёжен для гладких функций.

### Хукка-Дживса
Прямой поиск без производных, устойчив к шуму.

### Антиградиент
Классический градиентный спуск, быстр для выпуклых функций.

### Ньютона
Использует вторые производные, очень быстрая сходимость вблизи минимума.

## Производительность

- Кэширование производных для ускорения вычислений
- Эффективная работа с базой данных через контекстные менеджеры
- Оптимизированные математические операции через SymPy

## Лицензия

MIT License

## Контакты

Для вопросов и предложений создавайте Issues в репозитории.
